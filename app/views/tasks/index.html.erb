<ul>
<% @tasks.each do |task| %>
  <li> <%= link_to task.title, task_path(task) %>
    <button> <%= link_to "edit", edit_path(task.id) %></button> </li>
  <% end %>
  <button> <%= link_to "create new task", tasks_new_path %></button>
</ul>
